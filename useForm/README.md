# useForm Hook

Ejemplo de uso:
```
    const initialForm = {
        nameTest: '',
        emailTest: '',
        etc: ''
    }

    const [values, handleInputChange, resetValues] = useForm(initialForm);
```
